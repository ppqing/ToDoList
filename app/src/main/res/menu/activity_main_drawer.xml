<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_default"
            android:checked="true"
            android:icon="@drawable/ic_menu_gallery"
            android:title="默认" />
        <item
            android:id="@+id/nav_sortByTime"
            android:icon="@drawable/clock"
            android:title="生成工作计划" />
        <item
            android:id="@+id/nav_showCompleded"
            android:icon="@drawable/complete"
            android:title="已完成事件" />
        <item
            android:id="@+id/nav_showUncompleted"
            android:icon="@drawable/cancel"
            android:title="未完成事件" />
    </group>

    <item android:title="云服务">
        <menu>
            <item
                android:id="@+id/nav_download"
                android:icon="@android:drawable/stat_sys_download"
                android:title="从云端同步到本地" />
            <item
                android:id="@+id/nav_upload"
                android:icon="@android:drawable/stat_sys_upload"
                android:title="从本地同步到云端" />
        </menu>
    </item>

</menu>
